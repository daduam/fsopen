const initialMessage = 'notificaion reducer'

const notificaionReducer = (state = initialMessage, action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default notificaionReducer